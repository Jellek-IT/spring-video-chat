import { ChannelMemberRightPipe } from './channel-member-right.pipe';

describe('ChannelMemberRightPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelMemberRightPipe();
    expect(pipe).toBeTruthy();
  });
});
